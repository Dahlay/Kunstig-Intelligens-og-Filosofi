<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Hardened Runtime entitlements for notarization.
  Use with: codesign --entitlements entitlements.plist ...
-->
<plist version="1.0">
<dict>
  <!-- Allow JIT (not needed here, but kept for completeness) -->
  <!-- <key>com.apple.security.cs.allow-jit</key><true/> -->

  <!-- Allow unsigned + dyld env vars during dev; remove before shipping -->
  <!-- <key>com.apple.security.cs.disable-library-validation</key><true/> -->

  <!-- Audio input -->
  <key>com.apple.security.device.audio-input</key>
  <true/>

  <!-- Outgoing network (for future update checks) -->
  <!-- <key>com.apple.security.network.client</key><true/> -->

  <!-- Hardened runtime base -->
  <key>com.apple.security.app-sandbox</key>
  <false/>
</dict>
</plist>
